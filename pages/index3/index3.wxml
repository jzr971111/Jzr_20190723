<view>
  <view wx:for="{{arr}}" wx:key="{{index}}">
    {{index}}:{{item}}
  </view>

  <view wx:for="{{person}}" wx:key="{{index}}">
    {{index}}:{{item}}
  </view>

  <block wx:for="{{arr}}" wx:key="{{index}}" wx:for-index="i">
    {{i}}:{{item}}
  </block>
</view>